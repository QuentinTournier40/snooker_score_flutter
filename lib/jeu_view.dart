import 'package:flutter/cupertino.dart';

class JeuView extends StatelessWidget {
  const JeuView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("jeu"));
  }
}
